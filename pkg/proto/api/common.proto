syntax = "proto3";

//Union//

message Order {
    string column = 1;
    Direction direction = 2;
}

enum Direction {
    ASC = 0;
    DESC = 1;
}

message Pagination {
    int64 limit = 1;
    int64 offset = 2;
}
